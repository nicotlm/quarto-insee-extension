
```{r chargement_des_donnees}

library(dplyr)
library(DT)
library(plotly)

# View(starwars)

fr <- list(
  sProcessing = "Traitement en cours...", sSearch = "Rechercher&nbsp;:", 
  sLengthMenu = "Afficher _MENU_ &eacute;l&eacute;ments", 
  sInfo = "Affichage de l'&eacute;l&eacute;ment _START_ &agrave; _END_ sur _TOTAL_ &eacute;l&eacute;ments", 
  sInfoEmpty = "Affichage de l'&eacute;l&eacute;ment 0 &agrave; 0 sur 0 &eacute;l&eacute;ment", 
  sInfoFiltered = "(filtr&eacute; de _MAX_ &eacute;l&eacute;ments au total)", 
  sInfoPostFix = "", sLoadingRecords = "Chargement en cours...", 
  sZeroRecords = "Aucun &eacute;l&eacute;ment &agrave; afficher", 
  sEmptyTable = "Aucune donn&eacute;e disponible dans le tableau", 
  oPaginate = list(
    sFirst = "Premier", sPrevious = "Pr&eacute;c&eacute;dent", 
    sNext = "Suivant", sLast = "Dernier"
  ), 
  oAria = list(
    sSortAscending = ": activer pour trier la colonne par ordre croissant", 
    sSortDescending = ": activer pour trier la colonne par ordre d&eacute;croissant"
  )
)

moy_taille_poids_selon_sex <- starwars %>% 
  group_by(sex) %>% 
  summarise(avg_height = mean(height, na.rm = TRUE),
            avg_mass = mean(mass, na.rm = TRUE))

repartition_sex_genre <- starwars %>% count(sex, gender, sort = TRUE)


moy_taille_poids_selon_espece <- starwars %>% 
  group_by(species) %>% 
  summarise(avg_height = mean(height, na.rm = TRUE),
            avg_mass = mean(mass, na.rm = TRUE))


plot_taille_poids_par_sexe <- plot_ly(moy_taille_poids_selon_sex, 
                                      x = moy_taille_poids_selon_sex$sex, 
                                      y = moy_taille_poids_selon_sex$avg_height, 
                                      type = 'bar', 
                                      name = 'Taille (en cm)'
                                      )

plot_taille_poids_par_sexe <- plot_taille_poids_par_sexe %>% 
  add_trace(y = moy_taille_poids_selon_sex$avg_mass, 
            name = 'poids (en kg)')

plot_taille_poids_par_sexe <- plot_taille_poids_par_sexe %>% 
  layout(width = 1500, height = 500,
         yaxis = list(title = 'Moyenne'), 
         xaxis = list(title = 'Sexe'),
         barmode = 'group',
         title = "Poids et taille moyenne des personnages de Starwars selon le sexe",
         legend = list(x = 100, y = 0.5))

```
